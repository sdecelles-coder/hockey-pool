# Secrets locaux Streamlit (ne jamais committer)
.streamlit/secrets.toml

# Config locale (secrets ESPN + VERIFY_SSL propre à la machine) — voir .env.example
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS / éditeurs
.DS_Store
Thumbs.db
.vscode/
.idea/

# Scripts de test temporaires
test_*.py
blob.json

# Données personnelles (non versionnées)
draft_plan.json
lineup.json

# Override LOCAL du statut manuel (tests) — ne jamais committer.
# La référence officielle est player_status.json (commitée, lue par le Cloud).
player_status.local.json

# Données ESPN (rosters du pool) — rafraîchies à chaque ouverture de l'app, donc
# regénérées automatiquement. Ignorées pour ne pas polluer la référence git
# pendant les tests locaux.
espn*.json

modif à faire.md
